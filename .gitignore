# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Credenciales — NUNCA al repo
lib/config/constants.dart

# APK outputs
*.apk
*.aab

# Release signing — NUNCA al repo
*.keystore
*.jks
keystore/
android/key.properties

# Tokens y secrets locales — NUNCA al repo
.env.local
.env

# Supabase CLI binario (~100 MB, no debe ir al repo)
.tools/

# Android build cache
android/.gradle/
android/app/build/
android/local.properties

# Flutter plugins cache
.flutter-plugins

# Pubspec lock (opcional, pero recomendado para apps)
# pubspec.lock

# Source material de iconos (PNGs originales, SVGs, ICOs) — los assets
# finales viven en assets/icons/ que sí se trackean. Esta carpeta es solo
# para tener referencia local del material crudo.
/(1) G/
